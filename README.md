# 🖥️ Server Monitor Bot

Telegram-бот для мониторинга сервера на Linux. Предоставляет статистику по CPU, RAM, дискам, сети и общей информации о системе.

## ✨ Возможности

- 📊 **Общий статус** — быстрая сводка по основным метрикам
- 🔥 **CPU** — загрузка, частота, количество ядер
- 💾 **RAM** — использование оперативной памяти
- 💿 **Диски** — место на дисках и разделах
- 🌐 **Сеть** — трафик и сетевые интерфейсы
- ⚙️ **Система** — информация о платформе, uptime, температура

## 🚀 Быстрый старт

### 1️⃣ Получите токен бота

1. Откройте [@BotFather](https://t.me/BotFather) в Telegram
2. Отправьте команду `/newbot`
3. Следуйте инструкциям и получите токен
4. Сохраните токен в безопасном месте

### 2️⃣ Узнайте свой Telegram ID

1. Откройте [@userinfobot](https://t.me/userinfobot)
2. Отправьте любую команду
3. Скопируйте ваш ID (число)

### 3️⃣ Использование launcher.sh (рекомендуется)

```bash
# Клонируйте репозиторий
git clone https://github.com/apela812/server-stat-TG.git
cd server_monitor_bot

# Запустите универсальный launcher
./launcher.sh

# Или используйте CLI флаги:
./launcher.sh --setup      # Инициализировать всё с нуля
./launcher.sh --run        # Запустить бота напрямую
./launcher.sh --config     # Меню конфигурации
./launcher.sh --users      # Меню управления пользователями
./launcher.sh --help       # Справка
```

### 📋 Launcher Menu

Главное меню предоставляет доступ к:
- ⚙️ **Управление конфигурацией** — изменение токена, добавление администраторов по TG ID с подписями
- 👥 **Управление пользователями** — добавление/удаление пользователей с сохранением подписей
- 🔧 **Управление окружением** — проверка Python/pip, установка/обновление зависимостей
- 📊 **Состояние системы** — просмотр конфигурации, списка пользователей, установленных пакетов
- 🚀 **Запуск бота** — прямой запуск с проверками

### Альтернатива: Ручная установка

```bash
# Клонируйте репозиторий
git clone https://github.com/apela812/server-stat-TG.git
cd server_monitor_bot

# Создайте виртуальное окружение
python3 -m venv venv
source venv/bin/activate  # Linux/Mac
# или
venv\Scripts\activate  # Windows

# Установите зависимости
pip install -r requirements.txt

# Скопируйте файл окружения
cp .env.example .env

# Отредактируйте .env и вставьте ваш токен и ID
nano .env
```

Затем запустите бота:
```bash
python main.py
```

## 📁 Структура проекта

```
server_monitor_bot/
├── main.py              # Точка входа
├── config.py            # Конфигурация
├── requirements.txt     # Зависимости
├── .env.example         # Шаблон переменных окружения
├── .gitignore           # Игнорируемые файлы
├── handlers/            # Обработчики команд
│   ├── __init__.py
│   └── commands.py
├── keyboards/           # Клавиатуры
│   ├── __init__.py
│   └── main_kb.py
└── utils/               # Утилиты
    ├── __init__.py
    └── stats.py         # Сбор статистики сервера
```

## ⚙️ Конфигурация

Файл `.env`:

```env
# Telegram Bot Token (получите у @BotFather)
BOT_TOKEN=your_bot_token_here

# Список ID пользователей, которым разрешено использовать бота
ALLOWED_USERS=123456789,987654321
```

> Если `ALLOWED_USERS` пуст — бот доступен всем.

## 📋 Команды бота

| Команда | Описание |
|---------|----------|
| `/start` | Запустить бота, показать меню |
| `/status` | Общий статус сервера |
| `/cpu` | Статистика процессора |
| `/ram` | Статистика оперативной памяти |
| `/disk` | Статистика дисков |
| `/network` | Статистика сети |
| `/system` | Информация о системе |
| `/help` | Справка по командам |

## 🔧 Запуск в фоне (production)

### Systemd

Создайте файл `/etc/systemd/system/server-monitor-bot.service`:

```ini
[Unit]
Description=Server Monitor Telegram Bot
After=network.target

[Service]
Type=simple
User=your_user
WorkingDirectory=/path/to/server_monitor_bot
Environment="PATH=/path/to/venv/bin"
ExecStart=/path/to/venv/bin/python main.py
Restart=always

[Install]
WantedBy=multi-user.target
```

Запуск:

```bash
sudo systemctl daemon-reload
sudo systemctl enable server-monitor-bot
sudo systemctl start server-monitor-bot
sudo systemctl status server-monitor-bot
```

### Docker

Создайте `Dockerfile`:

```dockerfile
FROM python:3.11-slim

WORKDIR /app

COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY . .

CMD ["python", "main.py"]
```

Запуск:

```bash
docker build -t server-monitor-bot .
docker run -d --env-file .env server-monitor-bot
```

## 🛠️ Требования

- Python 3.9+
- Linux (Linux Mint, Ubuntu, Debian, etc.)
- Telegram Bot Token

## 📦 Зависимости

- `aiogram` — асинхронный фреймворк для Telegram Bot API
- `psutil` — кроссплатформенный модуль для информации о системе
- `python-dotenv` — загрузка переменных окружения из `.env`

## 🔒 Безопасность

- Бот проверяет ID пользователя перед выполнением команд
- Токен хранится в `.env` файле (не коммитьте в git!)
- Используйте `ALLOWED_USERS` для ограничения доступа

## 📝 Лицензия

MIT

## 🤝 Вклад

Pull Request приветствуются!

## 📞 Поддержка

Если у вас возникли проблемы, создайте Issue в репозитории.
# Updated on Ср 25 фев 2026 09:36:15 MSK
